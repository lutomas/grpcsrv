# The version of the generation template.
# Required.
# The only currently-valid value is v1beta1.
version: v1beta1

build:
  roots:
    - apis
lint:
  use:
    - DEFAULT
breaking:
  use:
    - WIRE_JSON